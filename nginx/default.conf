upstream api {
  server 127.0.0.1:8080;
}

upstream client {
  server 127.0.0.1:3030;
}

server {
  listen 80;

  location /api {
    proxy_pass http://api;
  }

  location / {
    proxy_pass http://client;
  }
}
